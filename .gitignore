node_modules/
dist/
.env
.env.*
!.env.example
*.local
*.pem
*.key
.cache/
.turbo/
.ai-backups/
.ai-workspace-state.json

# AI 工具本地配置（同步生成，不提交）
.claude/
.trae/
.workbuddy/

# 调试日志（本地生成，不提交）
debug.log

# 早期临时测试脚本（已被 release-gate-task-00X.mjs 替代）
release-gate-test.mjs

# TypeScript 增量构建缓存（tsconfig.json incremental: true 生成，不提交）
*.tsbuildinfo
